(jbuild_version 1)

(executables
 ((names (user))
  (public_names (opam-ci))
  (libraries (obi bos cmdliner fmt.cli logs.fmt fmt.tty logs.cli rresult dockerfile-opam textwrap))
  (package opam-ci)
))
