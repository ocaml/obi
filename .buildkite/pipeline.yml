steps:
  - label: 'Build'
    command:
      - 'rsync -av /src/ /home/opam/src/ && sudo chown -R opam /home/opam/src'
      - 'opam install --deps-only /home/opam/src'
    plugins:
      docker#v1.1.1:
        image: "ocaml/opam2-staging:debian-9-ocaml-4.06-linux-amd64"
        workdir: /src
        always_pull: true
        environment:
          - OPAMYES=1

